# Root Documentation Copies

These files were moved out of the repository root to keep the top-level tree focused on active project files while keeping historical copies discoverable for agents.

Prefer the live docs unless you specifically need these root-level copies:

- `docs/product/` for product and design specs.
- `docs/agent/` for agent plans, audits, contracts, and continuity logs.
