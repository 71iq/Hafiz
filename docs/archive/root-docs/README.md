# Archived Root Documentation

These files were moved out of the repository root to keep the top-level tree focused on active project files.

Live documentation now lives in:

- `docs/product/` for product and design specs.
- `docs/agent/` for agent plans, audits, contracts, and continuity logs.

Prefer the live docs unless you specifically need historical root-level copies.
